export const myApplicationsUrls = (url: string) => {
    switch (url) {
        case "ttk-epay-app-service-subscription":
            return "ttk-epay";
        default:
            return '';
    }
};